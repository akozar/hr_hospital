<odoo>
    <!-- List View -->
    <record id="hr_hospital_doctor_schedule_list" model="ir.ui.view">
        <field name="name">hr.hospital.doctor.schedule.list</field>
        <field name="model">hr.hospital.doctor.schedule</field>
        <field name="arch" type="xml">
            <list>
                <field name="doctor_id"/>
                <field name="day_of_week"/>
                <field name="date"/>
                <field name="time_from" widget="float_time"/>
                <field name="time_to" widget="float_time"/>
                <field name="schedule_type"/>
                <field name="notes"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="hr_hospital_doctor_schedule_form" model="ir.ui.view">
        <field name="name">hr.hospital.doctor.schedule.form</field>
        <field name="model">hr.hospital.doctor.schedule</field>
        <field name="arch" type="xml">
            <form string="Doctor Schedule">
                <group>
                    <group>
                        <field name="doctor_id"/>
                        <field name="day_of_week"/>
                        <field name="date"/>
                        <field name="schedule_type"/>
                    </group>
                    <group>
                        <field name="time_from" widget="float_time"/>
                        <field name="time_to" widget="float_time"/>
                        <field name="notes"/>
                    </group>
                </group>
            </form>
        </field>
    </record>

    <!-- Calendar View -->
    <record id="hr_hospital_doctor_schedule_calendar" model="ir.ui.view">
        <field name="name">hr.hospital.doctor.schedule.calendar</field>
        <field name="model">hr.hospital.doctor.schedule</field>
        <field name="arch" type="xml">
            <calendar string="Doctor Schedule" date_start="date" color="doctor_id" mode="month">
                <field name="doctor_id"/>
                <field name="time_from" widget="float_time"/>
                <field name="time_to" widget="float_time"/>
                <field name="schedule_type"/>
            </calendar>
        </field>
    </record>

    <!-- Action -->
    <record id="action_hr_hospital_doctor_schedule" model="ir.actions.act_window">
        <field name="name">Doctor Schedules</field>
        <field name="res_model">hr.hospital.doctor.schedule</field>
        <field name="view_mode">calendar,list,form</field>
    </record>

    <!-- Menu -->
    <menuitem id="hr_hospital_menu_doctor_schedule"
              parent="hr_hospital_menu_doctors"
              action="action_hr_hospital_doctor_schedule"
              name="Doctor Schedules"
              sequence="20"/>
</odoo>
